// #include <bits/stdc++.h>
#include "std++.h"
using namespace std;

int main(void)
{
    // ifstream in("teleport.in");
    // ofstream out("teleport.out");
    // int a,b,x,y,ans=0;
    // in >> a >> b >> x >> y;
    // ans += abs(a - min(x,y));
    // ans += abs(b - max(x,y));
    // out << ans;
    return 0;
}